这是一个基于STM32F407IGH6主控的平衡五连杆并联腿轮足机器人的代码。
我使用了FRSSRTOS和HAL库来编写，同时将一些运动学解算函数从MATLAB里面导到这个项目。
现在，我打算对整个项目进行重构。
并且对以下三个功能进行重点优化：
1.增强轮腿机器人的离地与触地检测功能。
2.重构机器人跳跃函数，使其能够根据机体倾斜程度，动态调整跳跃力的方向，并且跳跃力大小可手动指定；保证在不移动测试的时候跳跃力正常垂直于地面向上，
在前行的时候能顺利越过障碍物，足端触地时能平稳落地，不让机体倾角超过一定角度。
3.优化机器人对起伏路段的适应能力，使其做到快速收敛，能够更平稳地行走在不平坦的地形上。
4.优化机器人的位姿估算，我还疑惑我是否需要一个没有零漂的YAW轴角度，因为原来的6维度卡尔曼估计里面的YAW轴角度漂移比较严重，毕竟我后期会加上云台和摄像头。
5.机器人的重心因0.8kg的电池安装位置不同会有所偏移，计划增加一个调节模型，调节双腿支持力度，
以保证机器人的双腿跳跃支持力足够，不使其在空中发散；保证机器人在行走过程中重心稳定，机体不倾斜。
6.我原来的ObserveTask任务里面还实现了一些对机器人打滑的机制处理，还有对系统的卡尔曼估计，但是效果都不太好，这些可能也需要优化,
7.我还打算添加一个MPC模型预测控制模块，正好能使用ObserveTask任务里面估计出来的机器人位置和速度估计，用于优化机器人的运动控制策略，提高其在复杂环境（打滑）下的适应能力和稳定性。
你需要先对这些代码进行浏览，控制代码在Application子目录下，PID和滤波函数在Components子目录下，MATLAB仿真代码在Matlab_lib子目录下，
了解整体架构和各个模块的功能。先不要进行代码修改，可以先提示书面的参考意见和重构计划。
此外，我还计划优化机器人的能量管理系统，以延长其续航时间。
在重构过程中，我会确保代码的模块化和可维护性，方便后续的功能扩展和调试。
重构完成后，我将进行全面的测试，确保所有功能正常运行，并记录下测试结果以供参考。
我还漏了一点忘记和你说，我之前测试跳跃功能的时候，发现机器人很容易在空中发散（严重偏移水平面，pitch轴角度变化很大），并且在降落到地面的时候很生硬（理想情况下是类似弹簧那样来回吸收重力势能转化成的动能），导致它在触地的时候很容易直接倒地。

我要强调两点：不要随意给我添加文件，未经我的允许，禁止添加新的文件。STM32F407IGH6主控上面自带了BMI088的IMU和IST8310磁力计（仅有这两个传感器），我不会使用到别的传感器（视觉相机、超声波）。
1.关于电池重心（CG）补偿模型，不需要添加flash配置文件，直接在代码里面做宏定义即可，可以在D:\C_EX\stm32_hal_dev\rm_2025\wheel_leg_infantry\Components\Algorithm路径下添加重力补偿模型的.c/.h文件。其中包含校准函数，我会把计算出来的数据通过串口发送到上位机，然后记录数据并在代码里面写宏定义来使用。还要准备好在行走/跳跃前的 cg_compensator 的 ΔF 前馈。接口尽量解耦。
2.关于ObserveTask 的打滑检测与卡尔曼估计优化，就按照你说的改进方向（分层策略）来做。可以在D:\C_EX\stm32_hal_dev\rm_2025\wheel_leg_infantry\Components\Algorithm路径下添加相应算法函数的.c/.h文件。接口尽量解耦。我再拿到ObserveTask 任务里面运行。
3.关于MPC 模型预测控制模块设计。按照你说的分层MPC来做。可以在D:\C_EX\stm32_hal_dev\rm_2025\wheel_leg_infantry\Components\Algorithm路径下添加相应算法函数的.c/.h文件。再新建一个MPC_ask任务来运行这个算法。接口尽量解耦。在板上实现在线求解，使用简化版 QP（小变量、固定N）和有限次迭代，保证实时性；
4.我觉得系统观测器很重要。如果要新增算法之类的东西，建议你在D:\C_EX\stm32_hal_dev\rm_2025\wheel_leg_infantry\Components\Algorithm路径下添加相应算法函数的.c/.h文件。接口尽量解耦。
在我没有添加CG偏移模型算法的时候，我仅仅只是通过补偿roll角度，使得机体在静止状态下保持水平。

/**
 * @brief 四元数转欧拉角 (ZYX旋转顺序)
 * @param q 输入四元数 [w, x, y, z]
 * @param euler 输出欧拉角 [0]=Yaw(Z), [1]=Roll(Y), [2]=Pitch(X)
 *
 * 旋转顺序: Z(Yaw) → Y(Roll) → X(Pitch)
 * 符合你的索引定义:
 *   euler[0] = Yaw (绕Z轴)
 *   euler[1] = Roll (绕Y轴)
 *   euler[2] = Pitch (绕X轴)
 */
// void Quaternion_To_Euler(const float q[4], float euler[3])
// {
//     float qw = q[0], qx = q[1], qy = q[2], qz = q[3];
//
//     // 1. Yaw (Z轴) - euler[0]
//     float siny_cosp = 2.0f * (qw * qz + qx * qy);
//     float cosy_cosp =  2.0f * (qw * qw + qx * qx) -1.0f ;
//     euler[0] = atan2f(siny_cosp, cosy_cosp);
//
//     // 2. Roll (Y轴) - euler[1]
//     float sinr = -2.0f * ( qz * qx - qw * qy);
//     // if (fabsf(sinr) >= 1.0f)
//     //     euler[1] = copysignf(M_PI / 2.0f, sinr);
//     // else
//     euler[1] = asinf(sinr);
//
//     // 3. Pitch (X轴) - euler[2]
//     float sinp_cosp = 2.0f * (qw * qx + qy * qz);
//     float cosp_cosp =  2.0f * (qw * qw + qz * qz) - 1.0f;
//     euler[2] = atan2f(sinp_cosp, cosp_cosp);
// }